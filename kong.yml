_format_version: "3.0"
_transform: true

# JWT consumer for access token verification (HS256)
consumers:
  - username: lms-access-consumer
  - username: lms-anonymous-consumer

jwt_secrets:
  - consumer: lms-access-consumer
    key: access
    algorithm: HS256
    secret: lms-dev-gateway-secret-2026-min-32chars

services:
  - name: auth-service
    url: http://host.docker.internal:3101
    connect_timeout: 2000
    read_timeout: 5000
    write_timeout: 5000
    retries: 0
    routes:
      - name: auth-public-route
        paths:
          - /auth
        strip_path: true
        methods: [GET, POST, OPTIONS]
        plugins:
          - name: rate-limiting
            config:
              minute: 100
              policy: local
              fault_tolerant: true
              hide_client_headers: false
              error_code: 429
              error_message: "Too many requests. Please try again later."
          - name: pre-function
            config:
              access:
                - |
                  local method = kong.request.get_method()
                  local path = kong.request.get_path()
                  if method == "POST" then
                    if path == "/auth/login"
                      or path == "/auth/register"
                      or path == "/auth/refresh"
                      or path == "/auth/logout"
                    then
                      return
                    end
                  elseif method == "GET" then
                    if path == "/auth/health"
                      or path == "/auth/livez"
                      or path == "/auth/readyz"
                    then
                      return
                    end
                  elseif method == "OPTIONS" then
                    return
                  end
                  return kong.response.exit(401, { success = false, code = 401, message = "Unauthorized", data = nil, trace_id = kong.request.get_header("x-trace-id") or "" })
          - name: request-transformer
            config:
              remove:
                headers:
                  - x-user-id
                  - x-user-role
                  - x-user-email

      - name: auth-protected-route
        paths:
          - /auth
        strip_path: true
        methods: [GET, POST, PUT, PATCH, DELETE, OPTIONS]
        headers:
          authorization:
            - "~*.+"
        plugins:
          - name: rate-limiting
            config:
              minute: 100
              policy: local
          - name: jwt
            config:
              key_claim_name: type
              claims_to_verify: [exp, nbf]
              run_on_preflight: false
              header_names: [authorization]
              uri_param_names: []
              cookie_names: []
          - name: request-transformer
            config:
              remove:
                headers:
                  - x-user-id
                  - x-user-role
                  - x-user-email
          - name: post-function
            config:
              access:
                - |
                  local jwt = kong.ctx.shared.authenticated_jwt_token
                  local payload = (jwt and jwt.payload) or (jwt and jwt.claims) or kong.ctx.shared.authenticated_jwt_claims or {}
                  kong.service.request.set_header("x-user-id", payload.userId or payload.user_id or "")
                  kong.service.request.set_header("x-user-role", payload.role or payload.user_role or "")
                  kong.service.request.set_header("x-user-email", payload.email or payload.user_email or "")

      - name: instructor-protected-route
        paths:
          - /instructor
        strip_path: true
        methods: [GET, POST, PUT, PATCH, DELETE, OPTIONS]
        headers:
          authorization:
            - "~*.+"
        plugins:
          - name: rate-limiting
            config:
              minute: 100
              policy: local
          - name: jwt
            config:
              key_claim_name: type
              claims_to_verify: [exp, nbf]
              run_on_preflight: false
              header_names: [authorization]
              uri_param_names: []
              cookie_names: []
          - name: request-transformer
            config:
              remove:
                headers:
                  - x-user-id
                  - x-user-role
                  - x-user-email
          - name: post-function
            config:
              access:
                - |
                  local jwt = kong.ctx.shared.authenticated_jwt_token
                  local payload = (jwt and jwt.payload) or (jwt and jwt.claims) or kong.ctx.shared.authenticated_jwt_claims or {}
                  kong.service.request.set_header("x-user-id", payload.userId or payload.user_id or "")
                  kong.service.request.set_header("x-user-role", payload.role or payload.user_role or "")
                  kong.service.request.set_header("x-user-email", payload.email or payload.user_email or "")

      - name: instructor-noauth-route
        paths:
          - /instructor
        strip_path: true
        methods: [GET, POST, PUT, PATCH, DELETE, OPTIONS]
        plugins:
          - name: request-transformer
            config:
              remove:
                headers:
                  - x-user-id
                  - x-user-role
                  - x-user-email
          - name: request-termination
            config:
              status_code: 401
              message: Unauthorized

  - name: course-service
    url: http://host.docker.internal:3002
    connect_timeout: 2000
    read_timeout: 5000
    write_timeout: 5000
    retries: 0
    routes:
      - name: course-protected-route
        paths:
          - /course
        strip_path: true
        methods: [GET, POST, PUT, PATCH, DELETE, OPTIONS]
        headers:
          authorization:
            - "~*.+"
        plugins:
          - name: rate-limiting
            config:
              minute: 100
              policy: local
          - name: jwt
            config:
              key_claim_name: type
              claims_to_verify: [exp, nbf]
              run_on_preflight: false
              header_names: [authorization]
              uri_param_names: []
              cookie_names: []
          - name: request-transformer
            config:
              remove:
                headers:
                  - x-user-id
                  - x-user-role
                  - x-user-email
          - name: post-function
            config:
              access:
                - |
                  local jwt = kong.ctx.shared.authenticated_jwt_token
                  local payload = (jwt and jwt.payload) or (jwt and jwt.claims) or kong.ctx.shared.authenticated_jwt_claims or {}
                  kong.service.request.set_header("x-user-id", payload.userId or payload.user_id or "")
                  kong.service.request.set_header("x-user-role", payload.role or payload.user_role or "")
                  kong.service.request.set_header("x-user-email", payload.email or payload.user_email or "")

      - name: course-public-route
        paths:
          - /course
        strip_path: true
        methods: [GET, OPTIONS]
        plugins:
          - name: rate-limiting
            config:
              minute: 100
              policy: local
          - name: request-transformer
            config:
              remove:
                headers:
                  - x-user-id
                  - x-user-role
                  - x-user-email
          - name: pre-function
            config:
              access:
                - |
                  local method = kong.request.get_method()
                  local path = kong.request.get_path()
                  if method == "GET" then
                    if path == "/course/health"
                      or path == "/course/livez"
                      or path == "/course/readyz"
                      or path == "/course/api/categories"
                      or path == "/course/api/courses"
                      or path:match("^/course/api/courses/[^/]+$")
                      or path:match("^/course/api/courses/[^/]+/reviews$")
                      or path:match("^/course/api/courses/[^/]+/reviews/stats$")
                      or path == "/course/api/instructors"
                      or path:match("^/course/api/instructors/[^/]+$")
                      or path:match("^/course/api/instructors/[^/]+/courses$")
                    then
                      return
                    end
                  end
                  return kong.response.exit(401, { success = false, code = 401, message = "Unauthorized", data = nil, trace_id = kong.request.get_header("x-trace-id") or "" })

  - name: learning-service
    url: http://host.docker.internal:3006
    connect_timeout: 2000
    read_timeout: 5000
    write_timeout: 5000
    retries: 0
    routes:
      - name: learning-protected-route
        paths:
          - /learning
        strip_path: true
        methods: [GET, POST, PUT, PATCH, DELETE, OPTIONS]
        headers:
          authorization:
            - "~*.+"
        plugins:
          - name: rate-limiting
            config:
              minute: 100
              policy: local
          - name: jwt
            config:
              key_claim_name: type
              claims_to_verify: [exp, nbf]
              run_on_preflight: false
              header_names: [authorization]
              uri_param_names: []
              cookie_names: []
          - name: request-transformer
            config:
              remove:
                headers:
                  - x-user-id
                  - x-user-role
                  - x-user-email
          - name: post-function
            config:
              access:
                - |
                  local jwt = kong.ctx.shared.authenticated_jwt_token
                  local payload = (jwt and jwt.payload) or (jwt and jwt.claims) or kong.ctx.shared.authenticated_jwt_claims or {}
                  kong.service.request.set_header("x-user-id", payload.userId or payload.user_id or "")
                  kong.service.request.set_header("x-user-role", payload.role or payload.user_role or "")
                  kong.service.request.set_header("x-user-email", payload.email or payload.user_email or "")

      - name: learning-public-route
        paths:
          - /learning
        strip_path: true
        methods: [GET, OPTIONS]
        plugins:
          - name: request-transformer
            config:
              remove:
                headers:
                  - x-user-id
                  - x-user-role
                  - x-user-email
          - name: pre-function
            config:
              access:
                - |
                  local path = kong.request.get_path()
                  if path == "/learning/health"
                    or path == "/learning/livez"
                    or path == "/learning/readyz"
                  then
                    return
                  end
                  return kong.response.exit(401, { success = false, code = 401, message = "Unauthorized", data = nil, trace_id = kong.request.get_header("x-trace-id") or "" })

  - name: community-service
    url: http://host.docker.internal:3007
    connect_timeout: 2000
    read_timeout: 5000
    write_timeout: 5000
    retries: 0
    routes:
      - name: community-protected-route
        paths:
          - /community
          - /qa
        strip_path: true
        methods: [GET, POST, PUT, PATCH, DELETE, OPTIONS]
        headers:
          authorization:
            - "~*.+"
        plugins:
          - name: rate-limiting
            config:
              minute: 100
              policy: local
          - name: jwt
            config:
              key_claim_name: type
              claims_to_verify: [exp, nbf]
              run_on_preflight: false
              header_names: [authorization]
              uri_param_names: []
              cookie_names: []
          - name: request-transformer
            config:
              remove:
                headers:
                  - x-user-id
                  - x-user-role
                  - x-user-email
          - name: post-function
            config:
              access:
                - |
                  local jwt = kong.ctx.shared.authenticated_jwt_token
                  local payload = (jwt and jwt.payload) or (jwt and jwt.claims) or kong.ctx.shared.authenticated_jwt_claims or {}
                  kong.service.request.set_header("x-user-id", payload.userId or payload.user_id or "")
                  kong.service.request.set_header("x-user-role", payload.role or payload.user_role or "")
                  kong.service.request.set_header("x-user-email", payload.email or payload.user_email or "")

      - name: community-public-route
        paths:
          - /community
          - /qa
        strip_path: true
        methods: [GET, OPTIONS]
        plugins:
          - name: request-transformer
            config:
              remove:
                headers:
                  - x-user-id
                  - x-user-role
                  - x-user-email
          - name: pre-function
            config:
              access:
                - |
                  local path = kong.request.get_path()
                  if path == "/community/health"
                    or path == "/community/livez"
                    or path == "/community/readyz"
                    or path == "/qa/health"
                    or path == "/qa/livez"
                    or path == "/qa/readyz"
                  then
                    return
                  end
                  return kong.response.exit(401, { success = false, code = 401, message = "Unauthorized", data = nil, trace_id = kong.request.get_header("x-trace-id") or "" })

  - name: payment-service
    url: http://host.docker.internal:3003
    connect_timeout: 2000
    read_timeout: 5000
    write_timeout: 5000
    retries: 0
    routes:
      - name: payment-protected-route
        paths:
          - /payment
        strip_path: true
        methods: [GET, POST, PUT, PATCH, DELETE, OPTIONS]
        headers:
          authorization:
            - "~*.+"
        plugins:
          - name: rate-limiting
            config:
              minute: 200
              policy: local
          - name: jwt
            config:
              key_claim_name: type
              claims_to_verify: [exp, nbf]
              run_on_preflight: false
              header_names: [authorization]
              uri_param_names: []
              cookie_names: []
          - name: request-transformer
            config:
              remove:
                headers:
                  - x-user-id
                  - x-user-role
                  - x-user-email
          - name: post-function
            config:
              access:
                - |
                  local jwt = kong.ctx.shared.authenticated_jwt_token
                  local payload = (jwt and jwt.payload) or (jwt and jwt.claims) or kong.ctx.shared.authenticated_jwt_claims or {}
                  kong.service.request.set_header("x-user-id", payload.userId or payload.user_id or "")
                  kong.service.request.set_header("x-user-role", payload.role or payload.user_role or "")
                  kong.service.request.set_header("x-user-email", payload.email or payload.user_email or "")

      - name: payment-public-route
        paths:
          - /payment
        strip_path: true
        methods: [GET, POST, OPTIONS]
        plugins:
          - name: request-transformer
            config:
              remove:
                headers:
                  - x-user-id
                  - x-user-role
                  - x-user-email
          - name: pre-function
            config:
              access:
                - |
                  local method = kong.request.get_method()
                  local path = kong.request.get_path()
                  if path == "/payment/health"
                    or path == "/payment/livez"
                    or path == "/payment/readyz"
                  then
                    return
                  end
                  if path == "/payment/api/vnpay-return" and (method == "GET" or method == "POST") then
                    return
                  end
                  if path == "/payment/api/vnpay-ipn" and (method == "GET" or method == "POST") then
                    return
                  end
                  return kong.response.exit(401, { success = false, code = 401, message = "Unauthorized", data = nil, trace_id = kong.request.get_header("x-trace-id") or "" })

  - name: media-service
    url: http://host.docker.internal:3004
    connect_timeout: 2000
    read_timeout: 5000
    write_timeout: 5000
    retries: 0
    routes:
      - name: media-protected-route
        paths:
          - /media
        strip_path: true
        methods: [GET, POST, PUT, PATCH, DELETE, OPTIONS]
        headers:
          authorization:
            - "~*.+"
        plugins:
          - name: rate-limiting
            config:
              minute: 100
              policy: local
          - name: jwt
            config:
              key_claim_name: type
              claims_to_verify: [exp, nbf]
              run_on_preflight: false
              header_names: [authorization]
              uri_param_names: []
              cookie_names: []
          - name: request-transformer
            config:
              remove:
                headers:
                  - x-user-id
                  - x-user-role
                  - x-user-email
          - name: post-function
            config:
              access:
                - |
                  local jwt = kong.ctx.shared.authenticated_jwt_token
                  local payload = (jwt and jwt.payload) or (jwt and jwt.claims) or kong.ctx.shared.authenticated_jwt_claims or {}
                  kong.service.request.set_header("x-user-id", payload.userId or payload.user_id or "")
                  kong.service.request.set_header("x-user-role", payload.role or payload.user_role or "")
                  kong.service.request.set_header("x-user-email", payload.email or payload.user_email or "")

      - name: media-public-route
        paths:
          - /media
        strip_path: true
        methods: [GET, PUT, OPTIONS]
        plugins:
          - name: request-transformer
            config:
              remove:
                headers:
                  - x-user-id
                  - x-user-role
                  - x-user-email
          - name: pre-function
            config:
              access:
                - |
                  local method = kong.request.get_method()
                  local path = kong.request.get_path()
                  if path == "/media/health"
                    or path == "/media/livez"
                    or path == "/media/readyz"
                  then
                    return
                  end
                  if method == "GET" and path:match("^/media/api/media/[^/]+$") then
                    return
                  end
                  if method == "GET" and path:match("^/media/api/media/lesson/[^/]+$") then
                    return
                  end
                  if method == "GET" and path:match("^/media/api/media/file/.+$") then
                    return
                  end
                  if method == "PUT" and path:match("^/media/api/upload/local/.+$") then
                    return
                  end
                  return kong.response.exit(401, { success = false, code = 401, message = "Unauthorized", data = nil, trace_id = kong.request.get_header("x-trace-id") or "" })

  - name: notification-service
    url: http://host.docker.internal:3005
    connect_timeout: 2000
    read_timeout: 5000
    write_timeout: 5000
    retries: 0
    routes:
      - name: notification-protected-route
        paths:
          - /notification
        strip_path: true
        methods: [GET, POST, PUT, PATCH, DELETE, OPTIONS]
        headers:
          authorization:
            - "~*.+"
        plugins:
          - name: rate-limiting
            config:
              minute: 100
              policy: local
          - name: jwt
            config:
              key_claim_name: type
              claims_to_verify: [exp, nbf]
              run_on_preflight: false
              header_names: [authorization]
              uri_param_names: []
              cookie_names: []
          - name: request-transformer
            config:
              remove:
                headers:
                  - x-user-id
                  - x-user-role
                  - x-user-email
          - name: post-function
            config:
              access:
                - |
                  local jwt = kong.ctx.shared.authenticated_jwt_token
                  local payload = (jwt and jwt.payload) or (jwt and jwt.claims) or kong.ctx.shared.authenticated_jwt_claims or {}
                  kong.service.request.set_header("x-user-id", payload.userId or payload.user_id or "")
                  kong.service.request.set_header("x-user-role", payload.role or payload.user_role or "")
                  kong.service.request.set_header("x-user-email", payload.email or payload.user_email or "")

      - name: notification-public-route
        paths:
          - /notification
        strip_path: true
        methods: [GET, OPTIONS]
        plugins:
          - name: request-transformer
            config:
              remove:
                headers:
                  - x-user-id
                  - x-user-role
                  - x-user-email
          - name: pre-function
            config:
              access:
                - |
                  local path = kong.request.get_path()
                  if path == "/notification/health"
                    or path == "/notification/livez"
                    or path == "/notification/readyz"
                  then
                    return
                  end
                  return kong.response.exit(401, { success = false, code = 401, message = "Unauthorized", data = nil, trace_id = kong.request.get_header("x-trace-id") or "" })

  - name: ai-service
    url: http://host.docker.internal:3008
    connect_timeout: 2000
    read_timeout: 5000
    write_timeout: 5000
    retries: 0
    routes:
      - name: ai-protected-route
        paths:
          - /ai
        strip_path: true
        methods: [GET, POST, PUT, PATCH, DELETE, OPTIONS]
        headers:
          authorization:
            - "~*.+"
        plugins:
          - name: rate-limiting
            config:
              minute: 100
              policy: local
          - name: jwt
            config:
              key_claim_name: type
              claims_to_verify: [exp, nbf]
              run_on_preflight: false
              header_names: [authorization]
              uri_param_names: []
              cookie_names: [accessToken]
          - name: request-transformer
            config:
              remove:
                headers:
                  - x-user-id
                  - x-user-role
                  - x-user-email
          - name: post-function
            config:
              access:
                - |
                  local jwt = kong.ctx.shared.authenticated_jwt_token
                  local payload = (jwt and jwt.payload) or (jwt and jwt.claims) or kong.ctx.shared.authenticated_jwt_claims or {}
                  kong.service.request.set_header("x-user-id", payload.userId or payload.user_id or "")
                  kong.service.request.set_header("x-user-role", payload.role or payload.user_role or "")
                  kong.service.request.set_header("x-user-email", payload.email or payload.user_email or "")

      - name: ai-cookie-protected-route
        paths:
          - /ai
        strip_path: true
        methods: [GET, POST, PUT, PATCH, DELETE, OPTIONS]
        headers:
          cookie:
            - "~*accessToken="
        plugins:
          - name: rate-limiting
            config:
              minute: 100
              policy: local
          - name: jwt
            config:
              key_claim_name: type
              claims_to_verify: [exp, nbf]
              run_on_preflight: false
              header_names: [authorization]
              uri_param_names: []
              cookie_names: [accessToken]
          - name: request-transformer
            config:
              remove:
                headers:
                  - x-user-id
                  - x-user-role
                  - x-user-email
          - name: post-function
            config:
              access:
                - |
                  local jwt = kong.ctx.shared.authenticated_jwt_token
                  local payload = (jwt and jwt.payload) or (jwt and jwt.claims) or kong.ctx.shared.authenticated_jwt_claims or {}
                  kong.service.request.set_header("x-user-id", payload.userId or payload.user_id or "")
                  kong.service.request.set_header("x-user-role", payload.role or payload.user_role or "")
                  kong.service.request.set_header("x-user-email", payload.email or payload.user_email or "")

      - name: ai-public-route
        paths:
          - /ai
        strip_path: true
        methods: [GET, OPTIONS]
        plugins:
          - name: request-transformer
            config:
              remove:
                headers:
                  - x-user-id
                  - x-user-role
                  - x-user-email
          - name: pre-function
            config:
              access:
                - |
                  local method = kong.request.get_method()
                  if method == "OPTIONS" then
                    return
                  end
                  local path = kong.request.get_path()
                  if path == "/ai/health"
                    or path == "/ai/livez"
                    or path == "/ai/readyz"
                  then
                    return
                  end
                  return kong.response.exit(401, { success = false, code = 401, message = "Unauthorized", data = nil, trace_id = kong.request.get_header("x-trace-id") or "" })

plugins:
  - name: cors
    config:
      origins:
        - "http://localhost:3000"
      methods: [GET, POST, PUT, DELETE, PATCH, OPTIONS]
      headers: [Accept, Authorization, Content-Type, x-trace-id]
      exposed_headers: [x-trace-id, X-RateLimit-Limit, X-RateLimit-Remaining, X-RateLimit-Reset, X-RateLimit-Limit-Minute, X-RateLimit-Remaining-Minute, Retry-After]
      credentials: true
      max_age: 3600

  - name: correlation-id
    config:
      header_name: x-trace-id
      generator: uuid
      echo_downstream: true
